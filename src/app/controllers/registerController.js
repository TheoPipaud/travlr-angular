'use strict';

angular
    .module('moviesapp.controllers.register', [])
    .controller('registerCtrl', [
        '$scope',
        function($scope) {
            console.log('registerCtrl');

        }
    ]);
