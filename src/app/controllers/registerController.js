'use strict';

angular
    .module('travlrapp.controllers.register', [])
    .controller('registerCtrl', [
        '$scope', '$http',
        function($scope, $http) {

        	$scope.user = {};

        	
        }
    ]);

